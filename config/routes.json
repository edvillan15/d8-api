{
  "routes": [
    {
      "method": "POST",
      "path": "/webhooks/stripe",
      "handler": "webhooks.stripeWebhook",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/webhooks/stripe",
      "handler": "webhooks.stripeWebhook",
      "config": {
        "policies": []
      }
    }
  ]
}
